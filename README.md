# Poetry-chatbot
Developed a poetry generation AI by fine-tuning LLMs using LoRA/PEFT. Built an end-to-end pipeline including data preprocessing, training, and evaluation, ensuring the model generates poems and rejects non-poetry queries.
